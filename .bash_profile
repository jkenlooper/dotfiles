# Only for the local machine use.

echo "cp this file to home directory of local machine and edit as needed"

# Update as needed
alias box='(source /path/to/my/environment-variables.sh; ssh -i /path/to/private_key/id_rsa jake@remote-box.example.com)'
